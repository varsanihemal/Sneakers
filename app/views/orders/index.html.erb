<h1>My Orders</h1>

<% if @orders.present? %>
  <ul>
    <% @orders.each do |order| %>
      <li>
        <%= link_to "Order ##{order.id}", order_path(order) %> - Total: <%= number_to_currency(order.total_amount) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No orders found.</p>
<% end %>
